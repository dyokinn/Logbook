import 'package:flutter/material.dart';

class LogPreview extends StatelessWidget {
  const LogPreview({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}